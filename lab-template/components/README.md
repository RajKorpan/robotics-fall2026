# Custom components

Add a component directory only when the lab needs interaction that standard Streamlit widgets cannot provide, such as drawing a route or recording an animated canvas.

A component should return a small serializable state object. Python remains responsible for mission evaluation and durable persistence.

